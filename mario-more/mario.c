#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int height;
    int spaces;
    int column;

    do
    {
       height = get_int("Height: ");
    }
    while (height < 1 || height > 8);

    for (int i = height; i > 0; i--)
    {
        printf(" ");
        for (int j = 0; height - j < height; j++)
        {

        }
    }

    for (int j = height )

}